# SimpleFantasyFootball - In Progress

### Usage

Clone the repository. You will need [Git][git], [Python 2.7][python], and [Pip][pip]. To install all other dependencies, run:
```sh
$  pip install -r requirements.txt
```